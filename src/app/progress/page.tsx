'use client';
import Link from 'next/link';
import { phases,weeks } from '@/data/course';
import { useCourseProgress } from '@/lib/progress';
import { ProgressBar } from '@/components/course';
export default function ProgressPage(){const {progress,stats,percent}=useCourseProgress();return <><div className="page-top"><div><span className="eyebrow">ONE SMALL STEP AT A TIME</span><h1>你的成长，看得见</h1><p>不需要赶进度，每一次学习都是向前的一步。</p></div></div>{stats.completed===24&&<div className="success">✧ 恭喜完成24周学习！愿这些知识陪伴你和家人的每一餐。</div>}<div className="stats-grid"><section className="panel"><p>总学习进度</p><strong className="stat-value">{stats.percentage}<small>%</small></strong><ProgressBar value={stats.percentage}/></section><section className="panel"><p>已完成课程</p><strong className="stat-value">{stats.completed}<small> / 24 周</small></strong></section><section className="panel"><p>必修清单完成</p><strong className="stat-value">{stats.checked}<small> / {stats.total} 项</small></strong></section></div><section className="panel resume-panel"><div><span className="eyebrow">CURRENT WEEK</span><h2>第 {progress.currentWeek} 周 · {weeks[progress.currentWeek-1].titleZh}</h2></div><Link className="button primary" href={`/course/week/${progress.currentWeek}`}>继续学习 →</Link></section><div className="section-title"><h2>每月学习进度</h2></div><div className="month-progress-grid">{phases.map(p=>{const items=weeks.filter(w=>w.month===p.month);const total=items.reduce((n,w)=>n+w.checklist.filter(c=>c.required).length,0);const checked=items.reduce((n,w)=>n+w.checklist.filter(c=>c.required&&progress.weeks[w.week]?.checks[c.id]).length,0);const percentage=Math.round(checked/total*100);return <section className="panel" key={p.month}><span className="eyebrow">MONTH 0{p.month}</span><div className="progress-label"><h3>{p.title}</h3><strong>{percentage}%</strong></div><ProgressBar value={percentage}/><div className="week-dots">{items.map(w=><Link className={percent(w.week)===100?'done':''} key={w.week} href={`/course/week/${w.week}`}>第{w.week}周 <span>{percent(w.week)}%</span></Link>)}</div><small>{checked} / {total} 项必修清单已完成</small></section>;})}</div><section className="panel activity"><h2>最近的学习足迹</h2>{progress.activity.length?progress.activity.slice(0,8).map((a,i)=><div key={i}><span className="activity-dot"/><Link href={`/course/week/${a.week}`}>第{a.week}周 · {a.text}</Link><time dateTime={a.date}>{new Date(a.date).toLocaleString('zh-CN',{month:'short',day:'numeric',hour:'2-digit',minute:'2-digit'})}</time></div>):<p>学习记录将从你完成第一项任务开始。今天就是很好的起点。</p>}</section></>}
